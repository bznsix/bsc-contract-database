�▒▒▒▒░░▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒╢╢╢╢╣╢╣╣╣╢╣╢▒▒▒▒▒▒╨%╖╖,          | |_) | (_| | |_) | |_| | | |  | |  __/ (_) \ V  V / 
          ░╢U░▓▓▓▓▓▓███████▓▓▓▓▓▓▓▓▓▓▓▓╫▓▓▓▓▓╣╢▒▒▒▒▒▒▒▒▒▓█▓▓█▓▓█▓▓▓▄▓▓▓╣▒▒▒▒╢▒▒▒▒▒▒▒╢╢╫╣╢╢╢▒╢▒▒▒▒▒▒▒╜╜╜╨╝╝▒▒─       |____/ \__,_|_.__/ \__, | |_|  |_|\___|\___/ \_/\_/ 
             ─▒▒╫▓▓▓▓▓▓▓███▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▀▒╜░░░░░░░╙░▒▒▒╢╢╢▒╢╢╢╢╢╢╣╢╢╢╢╢╢╢╣╢╣╣╣▒▒░'▒░   '                                 |___/                            


*/

// SPDX-License-Identifier: MIT
pragma solidity ^0.8.23;

library Address {
    function sendValue(address payable recipient, uint256 amount) internal returns(bool){
        require(address(this).balance >= amount, "Address: insufficient balance");

        (bool success, ) = recipient.call{value: amount}("");
        return success;
    }
}

interface IERC20 {
    function totalSupply() external view returns (uint256);
    function balanceOf(address account) external view returns (uint256);
    function transfer(address recipient, uint256 amount) external returns (bool);
    function allowance(address owner, address spender) external view returns (uint256);
    function approve(address spender, uint256 amount) external returns (bool);
    function transferFrom(address sender, address recipient, uint256 amount) external returns (bool);
   
    event Transfer(address indexed from, address indexed to, uint256 value);
    event Approval(address indexed owner, address indexed spender, uint256 value);
}

interface IERC20Metadata {
    function name() external view returns (string memory);
    function symbol() external view returns (string memory);
    function decimals() external view returns (uint8);
}

interface IRouter01 {
    function factory() external pure returns (address);
    function WETH() external pure returns (address);
    function addLiquidityETH(
        address token,
        uint amountTokenDesired,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external payable returns (uint amountToken, uint amountETH, uint liquidity);
    function addLiquidity(
        address tokenA,
        address tokenB,
        uint amountADesired,
        uint amountBDesired,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB, uint liquidity);
    function swapExactETHForTokens(
        uint amountOutMin, 
        address[] calldata path, 
        address to, uint deadline
    ) external payable returns (uint[] memory amounts);
    function getAmountsOut(uint amountIn, address[] calldata path) external view returns (uint[] memory amounts);
    function getAmountsIn(uint amountOut, address[] calldata path) external view returns (uint[] memory amounts);
}

interface IRouter02 is IRouter01 {
    function swapExactTokensForETHSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
    function swapExactETHForTokensSupportingFeeOnTransferTokens(
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external payable;
    function swapExactTokensForTokensSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
    function swapExactTokensForTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external returns (uint[] memory amounts);
}

interface IFactoryV2 {
    event PairCreated(address indexed token0, address indexed token1, address lpPair, uint);
    function getPair(address tokenA, address tokenB) external view returns (address lpPair);
    function createPair(address tokenA, address tokenB) external returns (address lpPair);
}

abstract contract Context {
    constructor() {
    }

    function _msgSender() internal view returns (address payable) {
        return payable(msg.sender);
    }

    function _msgData() internal view returns (bytes memory) {
        this;
        return msg.data;
    }
}

abstract contract Ownable is Context {
    address private _owner;

    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

    constructor() {
        _setOwner(_msgSender());
    }

    function owner() public view virtual returns (address) {
        return _owner;
    }

    modifier onlyOwner() {
        require(owner() == _msgSender(), "Ownable: caller is not the owner");
        _;
    }

    function renounceOwnership() public virtual onlyOwner {
        _setOwner(address(0));
    }

    function transferOwnership(address newOwner) public virtual onlyOwner {
        require(newOwner != address(0), "Ownable: new owner is the zero address");
        _setOwner(newOwner);
    }

    function _setOwner(address newOwner) private {
        address oldOwner = _owner;
        _owner = newOwner;
        emit OwnershipTransferred(oldOwner, newOwner);
    }
}

contract BabyMeow is Context, Ownable, IERC20, IERC20Metadata  {
    using Address for address payable;

    address payable private marketingWallet;
    address payable private dev1Wallet;
    address payable private dev2Wallet;

    mapping (address => bool) private noFee; 
    mapping (address => bool) private liquidityAdd;
    mapping (address => bool) private isPresaleAddress;
    mapping (address => uint256) private balances;
    mapping (address => mapping (address => uint256)) private _allowances;

    string constant private _name = "Baby Meow";
    string constant private _symbol = "BabyMeow";
    uint8 constant private _decimals = 9;

    uint256 private constant devAllocation = 90;
    uint256 private constant liquidityAllocation = 10;
    
    uint256 constant public _totalSupply = 420_690_000_000 * 10**9;
    uint256 constant public swapThreshold = _totalSupply / 2_000;
    uint256 constant public fee_denominator = 1_000;
    uint256 constant public buyfee = 50;
    uint256 constant public sellfee = 50;
    uint256 constant public transferfee = 0;

    address constant public DEAD = 0x000000000000000000000000000000000000dEaD;
    address immutable public lpPair;
    IRouter02 immutable public swapRouter;

    bool private canSwapFees = true;
    bool public isTradingEnabled = false;
    
    bool private inSwap;

        modifier inSwapFlag {
        inSwap = true;
        _;
        inSwap = false;

    }

    event EnableTrading(); 
    event SetPresaleAddress(address account, bool enabled);
    event ToggleCanSwapFees(bool enabled);
    event ChangeWallets(address newMarketing, address newDev1, address newDev2);
    event SwapAndLiquify(uint256 tokensSwapped, uint256 bnbReceived, uint256 tokensIntoLiqudity);
    event SwapAndSendMarketingAndDevelopment(uint256 tokensSwapped, uint256 marketingShare, uint256 dev1Share, uint256 dev2Share);
    event FailedToSwapTokensForETH();
    event SetNoFeeWallet(address account, bool enabled);
    

    constructor () {
        require(devAllocation + liquidityAllocation == 100, "BabyMeow: Invalid allocation sum");

        if (block.chainid == 56) {
            swapRouter = IRouter02(0x10ED43C718714eb63d5aA57B78B54704E256024E);
        } else if (block.chainid == 97) {
            swapRouter = IRouter02(0xD99D1c33F9fC3444f8101754aBC46c52416550D1);
        } else if (block.chainid == 1 || block.chainid == 5 || block.chainid == 31337) {
            swapRouter = IRouter02(0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D);
        } else {
            revert("BabyMeow: Invalid chain");
        }
         
        noFee[_msgSender()] = true;
        liquidityAdd[_msgSender()] = true;
        balances[_msgSender()] = _totalSupply;

        emit Transfer(address(0), _msgSender(), _totalSupply);

        lpPair = IFactoryV2(swapRouter.factory()).createPair(swapRouter.WETH(), address(this));

        _approve(_msgSender(), address(swapRouter), type(uint256).max);
        _approve(address(this), address(swapRouter), type(uint256).max);
    }

    function isNoFeeWallet(address account) external view returns(bool) {
        return noFee[account];
    }

    function setNoFeeWallet(address account, bool enabled) public onlyOwner {
        noFee[account] = enabled;
        emit SetNoFeeWallet(account, enabled);
    }

    function isLimitedAddress(address from, address to) internal view returns (bool) {
        bool isLimited = from != owner()
            && to != owner()
            && _msgSender() != owner()
            && !liquidityAdd[from]  && !liquidityAdd[to] && to != address(0) && to != address(this);
            return isLimited;
    }

    function is_buy(address from, address to) internal view returns (bool) {
        return to != lpPair && from == lpPair;
    }

    function is_sell(address from, address to) internal view returns (bool) { 
        return to == lpPair && from != lpPair;
    }

    function canSwap(address from, address to) internal view returns (bool) {
        return canSwapFees && !isPresaleAddress[from] && !isPresaleAddress[to]; 
    }

    function toggleCanSwapFees(bool value) external onlyOwner {
        require(canSwapFees != value, "BabyMeow: Cannot set same value");
        canSwapFees = value;
        emit ToggleCanSwapFees(value);
    }
 
    function _transfer(address from, address to, uint256 amount) internal {
        bool takeFee = true; 
        require(to != address(0), "ERC20: transfer to the zero address");
        require(from != address(0), "ERC20: transfer from the zero address");
        require(balances[from] >= amount, "ERC20: transfer amount exceeds balance");
        
        if (isLimitedAddress(from,to)) {
            require(isTradingEnabled,"BabyMeow: Trading is not enabled");
        }

        if (amount == 0) {
            emit Transfer(from, to, 0);
            return;
        }

        if(is_sell(from, to) && !inSwap && canSwap(from, to)) {
            uint256 contractTokenBalance = balanceOf(address(this));
   
            if(contractTokenBalance >= swapThreshold) {
                if(devAllocation > 0) {
                    swapAndSendMarketingAndDevelopment(contractTokenBalance * devAllocation / 100);
                }
                    
                if(liquidityAllocation > 0) {
                    swapAndLiquify(contractTokenBalance * liquidityAllocation / 100);
                }
            }
        }

        if (noFee[from] || noFee[to]){
            takeFee = false;
        }

        balances[from] -= amount; 
        uint256 amountAfterFee = (takeFee) ? takeTaxes(from, is_buy(from, to), is_sell(from, to), amount) : amount;
        
        balances[to] += amountAfterFee;
        emit Transfer(from, to, amountAfterFee);
    }

    function takeTaxes(address from, bool isBuy, bool isSell, uint256 amount) internal returns (uint256) {
        uint256 fee;
        if (isBuy)  fee = buyfee;  else if (isSell)  fee = sellfee;  else  fee = transferfee; 
        if (fee == 0)  return amount; 
        
        uint256 feeAmount = amount * fee / fee_denominator;

        if (feeAmount > 0) {
            balances[address(this)] += feeAmount;
            emit Transfer(from, address(this), feeAmount);
            
        }

        return amount - feeAmount;
    }

    function swapAndLiquify(uint256 tokenAmount) internal inSwapFlag {
        uint256 firstMath = tokenAmount / 2;
        uint256 secondMath = tokenAmount - firstMath;

        uint256 initialBalance = address(this).balance;

        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = swapRouter.WETH();

        if (_allowances[address(this)][address(swapRouter)] != type(uint256).max) {
            _allowances[address(this)][address(swapRouter)] = type(uint256).max;
        }

        try swapRouter.swapExactTokensForETHSupportingFeeOnTransferTokens(
            firstMath,
            0, 
            path,
            address(this),
            block.timestamp
        ) {
            uint256 newBalance = address(this).balance - initialBalance;

            (uint256 amountToken, uint256 amountETH, uint256 liquidityAdded) = swapRouter.addLiquidityETH{value: newBalance} (
                address(this),
                secondMath,
                0,
                0,
                DEAD,
                block.timestamp
            );
        
            emit SwapAndLiquify(amountToken, amountETH, liquidityAdded); 

        } catch {
            emit FailedToSwapTokensForETH();
        }
    }

    function swapAndSendMarketingAndDevelopment(uint256 tokenAmount) internal inSwapFlag {
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = swapRouter.WETH();

        if (_allowances[address(this)][address(swapRouter)] != type(uint256).max) {
            _allowances[address(this)][address(swapRouter)] = type(uint256).max;
        }

        try swapRouter.swapExactTokensForETHSupportingFeeOnTransferTokens(
            tokenAmount,
            0,
            path,
            address(this),
            block.timestamp
        ) {
            uint256 contractBalance = address(this).balance;
            uint256 equalAmount = contractBalance / 3;

            if (equalAmount > 0) { 
                marketingWallet.sendValue(equalAmount);
                dev1Wallet.sendValue(equalAmount);
                dev2Wallet.sendValue(equalAmount);

                emit SwapAndSendMarketingAndDevelopment(tokenAmount, equalAmount, equalAmount, equalAmount);
            }

        } catch {
            emit FailedToSwapTokensForETH();
        }
    }

    function changeWallets(address newMarketing, address newDev1, address newDev2) external onlyOwner {
        require(newMarketing != address(0), "BabyMeow: Address Zero");
        require(newDev1 != address(0), "BabyMeow: Address Zero");
        require(newDev2 != address(0), "BabyMeow: Address Zero");
 
        marketingWallet = payable(newMarketing);
        dev1Wallet = payable(newDev1);
        dev2Wallet = payable(newDev2);

        emit ChangeWallets(newMarketing, newDev1, newDev2);
    }

    function setPresaleAddress(address presale, bool value) external onlyOwner {
        require(isPresaleAddress[presale] != value, "BabyMeow: Cannot set same value");
        isPresaleAddress[presale] = value;
        noFee[presale] = value;
        liquidityAdd[presale] = value;

        emit SetPresaleAddress(presale, value);
    }

    function enableTrading() external onlyOwner {
        require(!isTradingEnabled, "BabyMeow: Trading already enabled");
        isTradingEnabled = true;
        
        emit EnableTrading();
    }

    function name() external pure override returns (string memory) { 
        return _name; 
    }

    function symbol() external pure override returns (string memory) { 
        return _symbol; 
    }

    function decimals() external pure override returns (uint8) { 
        return _decimals;
    }
    
    function totalSupply() external pure override returns (uint256) { 
        return _totalSupply; 
    }

    function balanceOf(address account) public view override returns (uint256) {
        return balances[account];
    }

    function allowance(address holder, address spender) external view override returns (uint256) { 
        return _allowances[holder][spender];
    }

    function transfer(address recipient, uint256 amount) public virtual override returns (bool) {
        _transfer(_msgSender(), recipient, amount);
        return true;
    }

    function transferFrom(address sender, address recipient, uint256 amount) public virtual override returns (bool) {
        uint256 currentAllowance = _allowances[sender][_msgSender()];
        if (currentAllowance != type(uint256).max) {
            require(currentAllowance >= amount, "ERC20: transfer amount exceeds allowance");
            unchecked {
                _approve(sender, _msgSender(), currentAllowance - amount);
            }
        }

        _transfer(sender, recipient, amount);
        return true;
    }

    function approve(address spender, uint256 amount) public virtual override returns (bool) {
        _approve(_msgSender(), spender, amount);
        return true;
    }

    function _approve(address owner, address spender, uint256 amount) internal virtual {
        require(owner != address(0), "ERC20: approve from the zero address");
        require(spender != address(0), "ERC20: approve to the zero address");

        _allowances[owner][spender] = amount;
        emit Approval(owner, spender, amount);
    }

    function increaseAllowance(address spender, uint256 addedValue) public virtual returns (bool) {
        _approve(_msgSender(), spender, _allowances[_msgSender()][spender] + addedValue);
        return true;
    }

    function decreaseAllowance(address spender, uint256 subtractedValue) public virtual returns (bool) {
        uint256 currentAllowance = _allowances[_msgSender()][spender];
        require(currentAllowance >= subtractedValue, "ERC20: decreased allowance below zero");
        unchecked {
            _approve(_msgSender(), spender, currentAllowance - subtractedValue);
        }

        return true;
    }

    receive() external payable {}
}